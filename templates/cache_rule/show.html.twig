{% extends 'base.html.twig' %}

{% block title %}CacheRule{% endblock %}

{% block body %}
    <h1>CacheRule</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cache_rule.id }}</td>
            </tr>
            <tr>
                <th>JudgeType</th>
                <td>{{ cache_rule.judgeType }}</td>
            </tr>
            <tr>
                <th>JudgeCond</th>
                <td>{{ cache_rule.judgeCond }}</td>
            </tr>
            <tr>
                <th>ResType</th>
                <td>{{ cache_rule.resType }}</td>
            </tr>
            <tr>
                <th>ResCond</th>
                <td>{{ cache_rule.resCond }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cache_rule_index') }}">back to list</a>

    <a href="{{ path('app_cache_rule_edit', {'id': cache_rule.id}) }}">edit</a>

    {{ include('cache_rule/_delete_form.html.twig') }}
{% endblock %}
